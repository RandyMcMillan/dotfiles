name: makefile.yml

on:
  push:
    branches: [ "master", "main" , "**", "**/**" ]
  pull_request:
    branches: [ "master", "main" , "**", "**/**" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: |
          #make brew || echo "brew linux not supported..."
          #make
          make autoconf        
          #make keymap          
          #make init            
          ##make brew            
          #make iterm           
          #make help            
          #make nvm             
          #make vim             
          #make clean-nvm       
          #make legit           
          #make legit-clean     
          #make nostril         
          #make nostril-clean   
          #make venv            
          #make venv-test       
          #make venv-install    
