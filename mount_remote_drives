# https://jonathansblog.co.uk/sshfs-mount-remote-drive-in-finder

# give your drive a nice name and location
mkdir /Volumes/ssh.mine.nu

#mount it
sshfs -o reconnect -o volname=ssh.mine.nu -o IdentityFile=~/.ssh/my_git_key2 git@ssh.mine.nu:/ /Volumes/ssh.mine.nu

# give your drive a nice name and location
mkdir /Volumes/Movies

#mount it
sshfs -o reconnect -o volname=Movies -o IdentityFile=~/.ssh/my_git_key2 git@ssh.mine.nu:/Volumes/DeepSpaceExtHD/Movies /Volumes/Movies

# give your drive a nice name and location
mkdir /Volumes/Pico

#mount it
sshfs -o reconnect -o volname=Pico -o IdentityFile=~/.ssh/my_git_key2 git@ssh.mine.nu:/Users/git/Pico /Volumes/Pico

# give your drive a nice name and location
mkdir /Volumes/circe.rc.usf.edu

#mount it
sshfs -o reconnect -o volname=circe.rc.usf.edu -o IdentityFile=~/.ssh/id_rsa rmcmillan1@circe.rc.usf.edu:/home/r/rmcmillan1/ /Volumes/circe.rc.usf.edu

